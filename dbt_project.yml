name: dip_pipeline
version: "1.0.0"
config-version: 2
profile: dip_pipeline

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dip_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
